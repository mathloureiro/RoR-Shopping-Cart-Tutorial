<h1>Your cart</h1>
<%= link_to "Empty cart", cart_clear_path %>
<br><br><br>

<% @cart.each do |id, qty| %>
  <% product = Product.find_by_id(id) %>
  
  <ul>
  <li><%= link_to product.title, product %></li>
  </ul>

<% end %>